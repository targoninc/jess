{
  "name": "docs",
  "module": "src/server/server.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build-dev": "bun build src/server/server.ts --outdir ./out --sourcemap=linked --watch",
    "start-dev": "bun --watch src/index.ts",
    "build-prod": "bun build src/server/server.ts --outdir ./out --sourcemap=linked --minify",
    "start-prod": "bun src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@targoninc/jess": "^0.0.16",
    "@targoninc/jess-components": "^0.0.16",
    "dompurify": "^3.2.6",
    "marked": "^15.0.12"
  }
}
